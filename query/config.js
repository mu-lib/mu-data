define({
	/* The "id deliminator" token to distinguish resource type with it's ID */
	"id" : "!",
	/* The "accessor token" to access nested property on */
	"property" : ".",
	/* The "path terminator " token to indicate the start of a new property path */
	"path" : ",",
	/* The "query deliminator" token for joining multiple queries */
	"query" : "|"
});